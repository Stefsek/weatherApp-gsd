---
phase: 01-core-weather
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/weather_app/models/__init__.py, src/weather_app/models/weather_data.py, src/weather_app/utils/__init__.py, src/weather_app/utils/formatters.py, src/weather_app/utils/converters.py, src/weather_app/services/__init__.py, src/weather_app/services/geocoding.py, src/weather_app/services/weather.py, pyproject.toml]
autonomous: true
requirements: [CITY-01, CITY-02, TEMP-01, TEMP-02, TEMP-03, COND-01, COND-02, METR-01, METR-02, METR-03, ERR-01, ERR-02]
user_setup: []

must_haves:
  truths:
    - "Data models have proper type definitions for all weather fields"
    - "WMO weather codes map to human-readable text (0='Clear', 61='Moderate Rain', etc.)"
    - "Temperature converts accurately between Celsius and Fahrenheit"
    - "Wind direction converts from degrees to cardinal directions (0=N, 90=E, 45=NE)"
    - "Geocoding service returns coordinates for valid cities"
    - "Weather service fetches current conditions from Open-Meteo API"
    - "API errors are caught and converted to custom exceptions"
  artifacts:
    - path: "src/weather_app/models/weather_data.py"
      provides: "Type-safe dataclasses for WeatherData and GeocodingResult"
    - path: "src/weather_app/utils/formatters.py"
      provides: "format_weather_code() and format_wind_direction() functions"
    - path: "src/weather_app/utils/converters.py"
      provides: "celsius_to_fahrenheit() and format_temperature() functions"
    - path: "src/weather_app/services/geocoding.py"
      provides: "search_city() with Open-Meteo Geocoding API integration"
    - path: "src/weather_app/services/weather.py"
      provides: "get_current_weather() with @st.cache_data caching"
  key_links:
    - from: "services/geocoding.py"
      to: "models/weather_data.py"
      via: "returns GeocodingResult dataclass"
    - from: "services/weather.py"
      to: "models/weather_data.py"
      via: "returns WeatherData dataclass"
    - from: "services/weather.py"
      to: "utils/formatters.py"
      via: "uses format_weather_code for display"
    - from: "main.py (Plan 02)"
      to: "services/"
      via: "imports search_city and get_current_weather"
---

<objective>
Create foundation layer: data models, utilities, and API services with caching
</objective>

<execution_context>
@/Users/stefsek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefsek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/SUMMARY.md

Open-Meteo API endpoints:
- Geocoding: https://geocoding-api.open-meteo.com/v1/search?name={city}
- Weather: https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current=temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m,wind_direction_10m
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and data models</name>
  <files>src/weather_app/models/__init__.py, src/weather_app/models/weather_data.py</files>
  <action>
    Create the following directory structure:
    ```
    src/weather_app/
    ├── __init__.py
    ├── models/
    │   ├── __init__.py
    │   └── weather_data.py
    ├── services/
    │   ├── __init__.py
    │   ├── geocoding.py
    │   └── weather.py
    └── utils/
        ├── __init__.py
        ├── formatters.py
        └── converters.py
    ```

    In weather_data.py, create dataclasses:
    - `GeocodingResult`: name, latitude, longitude, country (optional)
    - `WeatherData`: temperature_c, humidity, wind_speed, wind_direction, weather_code, location_name

    Use @dataclass decorator with field types. Add __post_init__ for validation where needed.
  </action>
  <verify>
    <automated>python -c "from src.weather_app.models.weather_data import WeatherData, GeocodingResult; print('Models import OK')"</automated>
  </verify>
  <done>Dataclasses defined with proper types and validation</done>
</task>

<task type="auto">
  <name>Task 2: Create utility functions (formatters and converters)</name>
  <files>src/weather_app/utils/formatters.py, src/weather_app/utils/converters.py</files>
  <action>
    In formatters.py:
    - Create WMO_WEATHER_CODES mapping (dict from WMO codes 0-99 to human-readable strings: 0="Clear", 1="Mainly Clear", 2="Partly Cloudy", 3="Overcast", 45="Fog", 48="Depositing Rime Fog", 51="Light Drizzle", 53="Moderate Drizzle", 55="Dense Drizzle", 61="Slight Rain", 63="Moderate Rain", 65="Heavy Rain", 71="Slight Snow", 73="Moderate Snow", 75="Heavy Snow", 80="Slight Rain Showers", 81="Moderate Rain Showers", 82="Violent Rain Showers", 95="Thunderstorm", 96="Thunderstorm with Hail", 99="Thunderstorm with Heavy Hail")
    - Function `format_weather_code(code: int) -> str` - returns human-readable text for WMO code
    - Function `format_wind_direction(degrees: int) -> str` - converts degrees to cardinal (0=N, 90=E, 180=S, 270=W, 45=NE, etc.)

    In converters.py:
    - Function `celsius_to_fahrenheit(celsius: float) -> float`
    - Function `format_temperature(celsius: float, unit: str) -> str` - returns formatted string with degree symbol
  </action>
  <verify>
    <automated>python -c "from src.weather_app.utils.formatters import format_weather_code, format_wind_direction; from src.weather_app.utils.converters import celsius_to_fahrenheit; assert format_weather_code(0) == 'Clear'; assert format_wind_direction(0) == 'N'; assert celsius_to_fahrenheit(0) == 32; print('Utils OK')"</automated>
  </verify>
  <done>All formatters and converters implemented and tested</done>
</task>

<task type="auto">
  <name>Task 3: Create API services with caching</name>
  <files>src/weather_app/services/geocoding.py, src/weather_app/services/weather.py</files>
  <action>
    In geocoding.py:
    - Function `search_city(city_name: str) -> GeocodingResult | None`
    - Use requests.get to call Open-Meteo Geocoding API
    - Parse JSON response, extract first result
    - Handle 404/city not found - return None
    - Wrap in try/except for network errors - raise custom exception

    In weather.py:
    - Function `get_current_weather(lat: float, lon: float) -> WeatherData`
    - Use requests.get to call Open-Meteo Weather API with current parameters
    - Parse JSON response, extract current weather data
    - Use @st.cache_data(ttl=300) decorator to cache results
    - Wrap in try/except for API errors

    Add custom exceptions:
    - `GeocodingError` - for geocoding API failures
    - `WeatherAPIError` - for weather API failures
    - `NetworkError` - for network connectivity issues
  </action>
  <verify>
    <automated>python -c "from src.weather_app.services.geocoding import search_city; from src.weather_app.services.weather import get_current_weather; print('Services import OK')"</automated>
  </verify>
  <done>Services with caching ready for UI integration</done>
</task>

</tasks>

<verification>
- All imports work without errors
- Unit tests pass for formatters
- Services can be imported and show correct function signatures
</verification>

<success_criteria>
Phase 1 foundation complete when:
1. Data models (WeatherData, GeocodingResult) defined with proper types
2. WMO weather codes fully mapped to human-readable text
3. Temperature conversion works correctly
4. Wind direction conversion returns cardinal directions
5. Geocoding service returns coordinates for valid city
6. Weather service fetches and returns current weather data
7. Both services have error handling for API failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-weather/01-01-SUMMARY.md`
</output>
