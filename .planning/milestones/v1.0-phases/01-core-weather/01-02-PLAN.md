---
phase: 01-core-weather
plan: 02
type: execute
wave: 2
depends_on: [01-core-weather-01]
files_modified: [main.py, src/weather_app/__init__.py]
autonomous: true
requirements: [CITY-01, CITY-02, TEMP-01, TEMP-02, TEMP-03, COND-01, COND-02, METR-01, METR-02, METR-03, ERR-01, ERR-02]
user_setup: []

must_haves:
  truths:
    - "User can search for any city by name and see weather results"
    - "User sees 'city not found' error when searching an invalid city"
    - "User sees current temperature in Celsius by default"
    - "User can toggle between Celsius and Fahrenheit; preference persists during session"
    - "User sees current weather condition as human-readable text"
    - "User sees humidity percentage"
    - "User sees wind speed"
    - "User sees wind direction as cardinal direction (N, NE, E, etc.)"
    - "App displays user-friendly error messages when API calls fail"
    - "App displays user-friendly error messages when network errors occur"
  artifacts:
    - path: "main.py"
      provides: "Complete Streamlit UI with city search, weather display, unit toggle, error handling"
  key_links:
    - from: "main.py"
      to: "services/geocoding.py"
      via: "calls search_city() on form submit"
    - from: "main.py"
      to: "services/weather.py"
      via: "calls get_current_weather() with lat/lon"
    - from: "main.py"
      to: "utils/formatters.py"
      via: "imports format_weather_code, format_wind_direction"
    - from: "main.py"
      to: "utils/converters.py"
      via: "imports format_temperature"
    - from: "main.py"
      to: "session_state"
      via: "stores temperature_unit preference"
---

<objective>
Create Streamlit UI with city search, weather display, and error handling
</objective>

<execution_context>
@/Users/stefsek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefsek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/SUMMARY.md
@.planning/phases/01-core-weather/01-01-SUMMARY.md (will be created after Plan 01)

Key imports from Plan 01:
- from src.weather_app.models.weather_data import WeatherData, GeocodingResult
- from src.weather_app.services.geocoding import search_city, GeocodingError
- from src.weather_app.services.weather import get_current_weather, WeatherAPIError
- from src.weather_app.utils.formatters import format_weather_code, format_wind_direction
- from src.weather_app.utils.converters import format_temperature
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main Streamlit application</name>
  <files>main.py, src/weather_app/__init__.py</files>
  <action>
    Create Streamlit app in main.py with the following structure:

    1. **Session State Initialization** (at top of file):
       - Initialize st.session_state['temperature_unit'] = 'C' if not exists
       - Initialize st.session_state['weather_data'] = None if not exists

    2. **UI Layout**:
       - st.title("Weather App")
       - st.text_input for city search with placeholder "Enter city name..."
       - Add a radio button or toggle for temperature unit (Celsius/Fahrenheit)

    3. **Search Flow**:
       - When user enters city and presses Enter:
         - Call search_city() from geocoding service
         - If city found: call get_current_weather(lat, lon)
         - Store result in session_state

    4. **Weather Display** (if data exists):
       - Location name as header
       - Temperature: use format_temperature() with selected unit
       - Weather condition: use format_weather_code()
       - Humidity: show as percentage
       - Wind speed: show with unit (km/h)
       - Wind direction: use format_wind_direction()

    5. **Error Handling**:
       - Display st.error() for:
         - City not found (GeocodingError)
         - API failures (WeatherAPIError)
         - Network errors (NetworkError)
       - Use try/except blocks around API calls

    6. **Type hints and imports**:
       - Add type hints to all functions
       - Import all needed functions from services and utils

    Create src/weather_app/__init__.py to expose key exports from submodules.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('main.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>Streamlit app runs with city search and weather display</done>
</task>

<task type="auto">
  <name>Task 2: Test full flow with real API</name>
  <files>main.py</files>
  <action>
    Verify the complete user flow works:
    1. Search for a valid city (e.g., "London")
    2. Verify weather data displays correctly
    3. Search for invalid city (e.g., "xyzabc123") and verify error message shows
    4. Toggle temperature unit and verify it changes

    Check that all requirements from REQUIREMENTS.md are met:
    - CITY-01: City search works
    - CITY-02: Invalid city shows error
    - TEMP-01: Temperature in Celsius shows
    - TEMP-02: Unit toggle works
    - TEMP-03: Preference persists during session
    - COND-01: Weather condition displays
    - COND-02: WMO codes mapped correctly
    - METR-01: Humidity shows
    - METR-02: Wind speed shows
    - METR-03: Wind direction shows as cardinal
    - ERR-01: API failures show friendly messages
    - ERR-02: Network errors show friendly messages
  </action>
  <verify>
    <automated>Run Streamlit app and test manually with: streamlit run main.py --server.headless true</automated>
  </verify>
  <done>All 12 requirements functional and verified</done>
</task>

</tasks>

<verification>
- Streamlit app starts without errors
- City search returns results for valid cities
- Invalid cities show appropriate error messages
- Temperature unit toggle works
- All weather metrics display correctly
- Error handling catches API and network failures
</verification>

<success_criteria>
Phase 1 complete when:
1. User can search for any city by name (CITY-01)
2. "City not found" error displays for invalid cities (CITY-02)
3. Temperature displays in Celsius by default (TEMP-01)
4. User can toggle between Celsius and Fahrenheit (TEMP-02)
5. Unit preference persists during session (TEMP-03)
6. Weather condition shows as human-readable text (COND-01, COND-02)
7. Humidity percentage displays (METR-01)
8. Wind speed displays (METR-02)
9. Wind direction shows as cardinal direction (METR-03)
10. API failures show user-friendly messages (ERR-01)
11. Network errors show user-friendly messages (ERR-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-weather/01-02-SUMMARY.md`
</output>
