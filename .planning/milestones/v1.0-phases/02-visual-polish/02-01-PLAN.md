---
phase: 02-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .streamlit/config.toml
  - main.py
autonomous: true
requirements: [DESN-01, DESN-02, DESN-03, DESN-04]
must_haves:
  truths:
    - User sees dark theme background (not pure black, softer grays)
    - User sees title with cyan-to-purple gradient
    - User sees app content centered in 400px max-width
  artifacts:
    - path: ".streamlit/config.toml"
      provides: "Dark theme configuration"
      contains: "base=\"dark\""
    - path: "main.py"
      provides: "Centered container layout with max-width 400px"
      contains: "max-width"
  key_links:
    - from: ".streamlit/config.toml"
      to: "Streamlit app"
      via: "Native theming"
      pattern: "theme.*dark"
    - from: "main.py"
      to: "Streamlit container"
      via: "st.container with CSS"
      pattern: "max-width.*400"
---

<objective>
Configure dark theme and create 400px centered layout container with gradient title.
</objective>

<context>
@.planning/phases/02-visual-polish/02-CONTEXT.md
@main.py

## Locked Decisions (NON-NEGOTIABLE)
- Dark theme via Streamlit config (`.streamlit/config.toml`) — not custom CSS injection
- Layout: Streamlit container (not HTML wrapper div)
- Entire app constrained to 400px max-width, centered
- Title gradient: cyan-to-purple (#00d4ff → #7c3aed)
- Background: Softer grays (lighter dark theme)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Streamlit config with dark theme</name>
  <files>.streamlit/config.toml</files>
  <action>
Create `.streamlit/config.toml` with dark theme configuration:
- [theme]
  - base="dark"
  - primaryColor="#00d4ff"
  - backgroundColor="#1a1a2e" (softer gray for main background)
  - secondaryBackgroundColor="#16213e" (slightly lighter for containers)
  - textColor="#e0e0e0"
- [runner]
  - fastReruns=false
Use Streamlit's native theming, NOT custom CSS.
  </action>
  <verify>
File exists at .streamlit/config.toml with valid TOML and dark theme settings.
  </verify>
  <done>Dark theme configured via Streamlit native config</done>
</task>

<task type="auto">
  <name>Task 2: Create centered 400px container layout</name>
  <files>main.py</files>
  <action>
In main.py, wrap entire app content in a centered container with max-width 400px:
```python
# After st.set_page_config, at start of main():
container = st.container()
with container:
    # All app content goes here
```
Use st.markdown with CSS to set max-width: 400px; margin: 0 auto; for the container.
Per CONTEXT: Use Streamlit container, NOT HTML wrapper div.
  </action>
  <verify>
App displays within 400px width, centered on page. Verify by temporarily adding border to container.
  </verify>
  <done>400px max-width container centered on page</done>
</task>

<task type="auto">
  <name>Task 3: Add gradient title styling</name>
  <files>main.py</files>
  <action>
Add HTML/CSS gradient title using st.markdown:
```python
st.markdown("""
<h1 style='text-align: center; 
   background: linear-gradient(to right, #00d4ff, #7c3aed);
   -webkit-background-clip: text;
   -webkit-text-fill-color: transparent;
   font-size: 2.5em;'>Weather App</h1>
""", unsafe_allow_html=True)
```
Replace existing st.title("Weather App") with this.
Per CONTEXT: Use exact colors #00d4ff → #7c3aed.
  </action>
  <verify>
Title displays with cyan-to-purple gradient. Verify in browser.
  </verify>
  <done>Title displays gradient from #00d4ff to #7c3aed</done>
</task>

</tasks>

<verification>
Run `streamlit run main.py` and verify:
1. Dark theme applied (background is dark gray, not pure black)
2. App content centered in 400px container
3. Title shows cyan-to-purple gradient
</verification>

<success_criteria>
- DESN-01: Dark theme with softer gray background applied
- DESN-02: Title has cyan-to-purple gradient (#00d4ff → #7c3aed)
- DESN-03: Exact color scheme from spec used
- DESN-04: Layout centered with 400px max-width
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-polish/02-01-SUMMARY.md`
</output>
